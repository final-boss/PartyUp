defmodule Addict.AddictView do
  use Phoenix.HTML
  use Phoenix.View, root: "web/templates/"
  # NOTE: Silences unused warning, import as needed
  # import Phoenix.Controller, only: [view_module: 1]
  # import PartyUp.Router.Helpers
end
